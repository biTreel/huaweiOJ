/* * Author: biTree * Date: June,12,2016 * 统计兔子个数 */#include <iostream>using namespace std;static int getTotalCount(int monthCount);int main(){    int n;    cin>>n;    long res=getTotalCount(n);    cout<<res<<endl;    return 0;}static int getTotalCount(int monthCount){    if(1==monthCount||2==monthCount)        return 1;    else        return getTotalCount(monthCount-1)+getTotalCount(monthCount-2);}