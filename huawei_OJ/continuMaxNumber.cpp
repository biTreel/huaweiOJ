/* * Author: biTree * Date: July,01,2016 */#include <iostream>#include <vector>#include <string>#include <utility>#include <algorithm>using namespace std;pair<string,size_t> continuMax(string);inline bool cmp(pair<string,size_t> p1, pair<string,size_t> p2){    return p1.second > p2.second;}int main(){    string str;    getline(cin,str);    pair<string,size_t> p=continuMax(str);    cout<<p.first<<","<<p.second<<endl;    return 0;}pair<string,size_t> continuMax(string str){    if(str.empty()){        //pOut=NULL;        return make_pair(str,0);    }    string::iterator p=str.begin();    string::iterator q=p;    size_t len=0;    vector< pair<string,size_t> > vp;    while(q != str.end()){        if(*q<'0' || '9'<*q){   //not a digit            q++;            continue;        }   //q point to the first digit        p=q;        while(isdigit(*q))            ++q;        len=q-p;        string tmp(p,p+len);        vp.push_back(make_pair(tmp,len));    }    sort(vp.begin(),vp.end(),cmp);    return vp[0];}