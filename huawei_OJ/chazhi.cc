/* * Author: biTree * Date: June,12,2016 */#include <iostream>#define uint unsigned intusing namespace std;int GetResult(uint num);int main(){    int n;    cin>>n;    int res=GetResult(n);    cout<<res<<endl;    return 0;}int GetResult(uint num){    int arr[4];    int max=0,min=0;    int i=3;    while(0<=i){        arr[i--]=num%10;        num/=10;    }    //max    int tmp=0;    for(int i=0;i!=3;++i){        tmp=arr[i];        for(int j=i+1;j!=4;++j){            if(tmp<arr[j]){                tmp=arr[j];                arr[j]=arr[i];                arr[i]=tmp;            }        }    }    max=arr[3]+arr[2]*10+arr[1]*100+arr[0]*1000;    min=arr[0]+arr[1]*10+arr[2]*100+arr[3]*1000;    //cout<<"max"<<max<<" "<<"min"<<min<<endl;    return max-min;}