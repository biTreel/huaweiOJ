/* * Author: biTree * Date: July 07,2016 * String division */#include <iostream>#include <string>#include <vector>#include <stdexcept>using namespace std;vector<string> get8Str(string *strArr,int len);int main(){    int n;    cin>>n;    int i=0;    string *strArr=new string[n];    while(i != n){        //getline(cin,strArr[i]);        cin>>strArr[i];        ++i;    }    vector<string> strVec=get8Str(strArr,n);    vector<string>::iterator it=strVec.begin();    for(;it!=strVec.end();++it)        cout<<*it<<endl;    delete []strArr;    return 0;}vector<string> get8Str(string *strArr,int len){    if(0==len || NULL==strArr)        throw runtime_error("string len equal to 0!");    vector<string> strVec;    //ch='0';    string tmp;    int i=0;    while(i != len){        if(strArr[i].empty())            //continue;            ++i;        size_t len=strArr[i].size();        size_t pos=0;        while(true){            if(len < 8){                tmp.append(strArr[i].substr(pos));                tmp.append(8-len,'0');                strVec.push_back(tmp);                tmp.clear();                break;            }            else{                //string::iterator it=strArr[i].begin();                while(len >= 8){                    tmp.append(strArr[i].substr(pos,8));                    strVec.push_back(tmp);                    tmp.clear();                    //strArr[i].erase(it,it+8);                    pos+=8;                    len-=8;                }                if(0==len)                    break;            }        }        ++i;    }    return strVec;}