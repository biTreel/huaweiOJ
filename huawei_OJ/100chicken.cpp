/* * Author: biTree * Date: June,28,2016 */#include <iostream>#include <vector>using namespace std;struct chicken{     int male;    int female;    int children;};int getResult(vector<chicken> &list);void print(const vector<chicken> list);int main(){    vector<chicken> cvec;    int anyV;    cin>>anyV;    if(!getResult(cvec));        print(cvec);    return 0;}int getResult(vector<chicken> &list){    if(0!=list.size())        return -1;    int m,f,c;    size_t i=0;    chicken ck;    for(m=0;m!=20;++m){        for(f=0;f!=34;++f){            for(c=0;c<101;c+=3){                int res=m*5+f*3+c/3;                int num=m+f+c;                if(100==res&& 100==num){                    ck.male=m;                    ck.female=f;                    ck.children=c;                    list.push_back(ck);                }            }        }    }    return 0;}void print(const vector<chicken> list){    for(size_t i=0;i!=list.size();++i)        cout<<list[i].male<<" "<<list[i].female<<" "<<list[i].children<<endl;}